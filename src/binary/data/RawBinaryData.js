export default class RawBinaryData {
    /** The header. */
     header;

    /** The first lrp. */
    firstLRP;

    /** The pos offset. */
    posOffset;

    /** The neg offset. */
    negOffset;

    /** The last lrp. */
    lastLRP;

    /** The last intermediate lrp. */
    lastClosedLineLRP;

    /** The intermediates. */
    intermediates;

    /** The abs coord. */
    absCoord;

    /** The rel coord. */
    relCoord;

    /** The abs coord. */
    absCenter;

    /** The abs coord. */
    absCoordUR;

    /** The abs coord. */
    absCoordLL;
};
